modules = ["python-3.12", "web", "nodejs-20"]
run = "uvicorn main:app --host 0.0.0.0 --port 3000"

[nix]
channel = "stable-25_05"
packages = ["cargo", "glibcLocales", "libiconv", "rustc"]

[deployment]
run = ["sh", "-c", "uvicorn main:app --host 0.0.0.0 --port 3000"]

[workflows]
runButton = "Run Server"

[[workflows.workflow]]
name = "Run Server"
author = 45170021
mode = "sequential"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "python main.py --server --host 0.0.0.0 --port 3000"

[[ports]]
localPort = 3000
externalPort = 80
